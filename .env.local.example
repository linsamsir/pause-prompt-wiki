# =============================================================
# Pause Prompt Wiki — local environment
# Copy this file to `.env.local` and fill in the real values.
# =============================================================

# Supabase project URL (Settings → API → Project URL)
NEXT_PUBLIC_SUPABASE_URL=https://mliqkpmezfniouagozye.supabase.co

# Supabase anon key (Settings → API → Project API keys → anon / public)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# The canonical URL where this app is reachable. Used for magic-link redirects
# and Open Graph metadata. Override in production (e.g. Vercel env).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
